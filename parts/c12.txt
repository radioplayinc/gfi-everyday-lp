tom: 0.5rem;
    letter-spacing: -0.02em;
  }
  .panel p { font-size: 0.92rem; color: #5a554c; margin-bottom: 0.75rem; }
  .bridge-list {
    margin: 1rem 0 1.25rem;
    padding-left: 1.1rem;
    font-size: 0.9rem; color: #3a3630;
  }
  .bridge-list li { margin-bottom: 0.45rem; }
  .panel-close {
    float: right;
    background: none; border: none; cursor: pointer;
    font-size: 1.25rem; color: #8a8578; line-height: 1;
  }

  /* —— 9 Status —— */
  #status {
    background: var(--black);
    display: none;
  }
  #status.is-visible { display: flex; }
  .stepper {
    margin-top: 1.75rem;
    display: grid; gap: 0;
    max-width: 480px;
  }
  .step {
    display: grid;
    grid-template-columns: 28px 1fr;
    gap: 0.85rem;
    padding-bottom: 1.25rem;
    position: relative;
  }
  .step:not(:last-child)::before {
    content: "";
    position: absolute;
    left: 13px; top: 28px; bottom: 0;
    width: 2px;
    background: var(--line);
  }
  .step-dot {
    width: 28px; height: 28px; border-radius: 50%;
    border: 2px solid var(--line);
    display: grid; place-items: center;
    font-family: var(--font-mono); font-size: 0.65rem;
    background: var(--charcoal);
    position: relative; z-index: 1;
  }
  .step.is-done .step-dot {
    background: var(--paper); border-color: var(--paper); color: var(--black);
  }
  .step.is-current .step-dot {
    border-color: var(--paper); color: var(--paper);
  }
  .step h3 { font-size: 0.95rem; font-weight: 600; }
  .step p { font-size: 0.8rem; color: var(--muted); margin-top: 0.15rem; }

  .footer {
    padding: 2.5rem 1.25rem 3rem;
    border-top: 1px solid var(--line);
    font-size: 0.75rem; color: var(--muted);
    text-align: center;
    background: var(--black);
  }
  .footer strong { color: rgba(255,255,255,.75); font-weight: 500; }
  .footer-legal { margin-top: 0.75rem; max-width: 42rem; margin-left: auto; margin-right: auto; line-height: 1.5; }
  .footer-certainty { margin-top: 0.5rem; color: rgba(255,255,255,.45); max-wi