t-weight: 800;
    font-size: clamp(1.85rem, 5vw, 3.25rem);
    line-height: 1.05;
    letter-spacing: -0.03em;
  }
  .subline {
    margin-top: 0.75rem;
    font-size: clamp(0.95rem, 2.2vw, 1.1rem);
    color: var(--muted);
    max-width: 36rem;
  }
  .line-tight { max-width: 22ch; }
  .motion-caption {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: clamp(1.2rem, 3.2vw, 1.75rem);
    text-align: center;
    margin-top: 1.25rem;
    letter-spacing: -0.02em;
  }
  .qualify {
    color: var(--paper);
    font-style: normal;
    font-weight: 700;
  }
  #stickiness .qualify,
  #quote .qualify {
    color: var(--ink);
  }
  .btn {
    display: inline-flex; align-items: center; justify-content: center; gap: 0.5rem;
    padding: 0.9rem 1.35rem;
    border-radius: 999px; border: none; cursor: pointer;
    font-weight: 600; font-size: 0.95rem;
    transition: transform .15s ease, background .15s ease, opacity .15s;
  }
  .btn:active { transform: scale(0.98); }
  /* Primary: white on dark; black on paper (context override below) */
  .btn-primary { background: var(--paper); color: var(--black); }
  .btn-primary:hover { background: var(--ink-gray); color: var(--paper); }
  #quote .btn-primary,
  .panel .btn-primary,
  #stickiness .btn-primary {
    background: var(--black);
    color: var(--paper);
  }
  #quote .btn-primary:hover,
  .panel .btn-primary:hover,
  #stickiness .btn-primary:hover {
    background: var(--ink-gray);
    color: var(--paper);
  }
  .btn-ghost {
    background: transparent; color: var(--paper);
    border: 1px solid var(--line);
  }
  .btn-ghost:hover { border-color: rgba(255,255,255,.45); color: var(--paper); }
  .btn-block { width: 100%; }
  .btn-row { display: flex; flex-wrap: wrap; gap: 0.75rem; margin-top: 1.5rem; }

  /* —— 0 Scatter hero (cutouts on pure black) —— */
  .scatter-root {
    position: relative;
    background: #000;
    color: var(--paper);
  }
  .scatter-pin {
    position: sticky;
    top: 0;
    height