ine);
    background: transparent;
    color: var(--paper);
    cursor: pointer;
    transition: border-color .15s, background .15s;
  }
  .quiz-opt:hover, .quiz-opt.is-selected {
    border-color: rgba(255,255,255,.4);
    background: rgba(255,255,255,.06);
  }
  .quiz-hint {
    margin-top: 0.85rem;
    font-size: 0.8rem; color: var(--muted);
  }
  .quiz-progress {
    font-family: var(--font-mono); font-size: 0.65rem; letter-spacing: 0.1em;
    color: var(--muted); margin-bottom: 0.75rem;
  }

  /* —— 7 Quote —— */
  #quote {
    background: var(--paper-soft);
    color: var(--ink);
    min-height: auto;
    padding-top: 4.5rem;
    padding-bottom: 4rem;
  }
  #quote .eyebrow { color: var(--muted-dark); }
  .quote-shell {
    margin-top: 1.25rem;
    display: none;
  }
  .quote-shell.is-visible { display: block; }
  .quote-card {
    background: var(--paper);
    border: 1px solid rgba(5,11,22,.16);
    border-radius: 12px;
    overflow: hidden;
    box-shadow: none;
    max-width: 640px;
  }
  .quote-hero {
    position: relative;
    background: var(--charcoal);
  }
  .quote-hero img {
    width: 100%;
    aspect-ratio: 16/11;
    object-fit: cover;
  }
  .quote-watermark {
    position: absolute; inset: auto 0.85rem 0.85rem auto;
    font-family: var(--font-mono); font-size: 0.6rem; letter-spacing: 0.1em;
    color: rgba(255,255,255,.85);
    background: rgba(0,0,0,.7);
    padding: 0.3rem 0.5rem; border-radius: 4px;
    border: 1px solid rgba(255,255,255,.18);
  }
  .quote-body { padding: 1.35rem 1.25rem 1.5rem; }
  .quote-title {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 1.65rem; line-height: 1.15; margin-bottom: 0.5rem;
    letter-spacing: -0.02em;
  }
  .quote-anchor {
    font-size: 0.85rem; color: #6a655c; margin-bottom: 1.15rem;
  }
  .quote-block {
    padding: 0.85rem 0;
    border-top: 1px solid var(--line-dark);
  }
  .quote-block:first-of-type { border-top: none; padding-top: 0; }
  .quote-label {
    font-family: v