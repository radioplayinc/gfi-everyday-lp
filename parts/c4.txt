  justify-content: center;
    gap: 0.02em;
    margin: 0;
    font-family: var(--font-display);
    font-weight: 800;
    font-size: clamp(2.4rem, 9vw, 5.5rem);
    letter-spacing: -0.045em;
    color: var(--paper);
    text-shadow: 0 2px 28px rgba(0,0,0,.55), 0 8px 40px rgba(0,0,0,.35);
    pointer-events: none;
    z-index: 3;
  }
  .hero-letter {
    display: inline-block;
    opacity: 0;
    transform: translateY(2.2rem);
  }
  .scatter-copy {
    text-align: center;
    padding: 1.1rem 1.35rem 1.2rem;
    width: min(100%, 36rem);
    max-width: 36rem;
    margin: 0 auto;
    position: relative;
    z-index: 4;
    pointer-events: auto;
    background: rgba(0,0,0,.68);
    border: 1px solid rgba(255,255,255,.14);
    border-radius: 18px;
    backdrop-filter: blur(8px) saturate(1.05);
    -webkit-backdrop-filter: blur(8px) saturate(1.05);
  }
  .scatter-copy .headline {
    color: var(--paper);
    font-size: clamp(1.45rem, 4vw, 2.35rem);
    max-width: 22ch;
    margin: 0 auto;
    text-shadow: 0 2px 24px rgba(0,0,0,.85);
  }
  .scatter-copy .sub-micro {
    margin-top: 0.65rem;
    font-size: 0.78rem;
    color: rgba(255,255,255,.84);
    letter-spacing: 0.01em;
  }
  .scatter-chip-row {
    display: flex; flex-wrap: wrap; gap: 0.45rem;
    justify-content: center; margin-top: 0.75rem;
  }
  .scatter-chip {
    font-family: var(--font-label);
    font-size: 0.62rem; font-weight: 600; letter-spacing: 0.16em;
    text-transform: uppercase;
    padding: 0.35rem 0.7rem;
    border-radius: 999px;
    border: 1px solid rgba(255,255,255,.35);
    color: var(--paper);
    background: rgba(0,0,0,.4);
  }
  .scatter-copy .btn-row {
    justify-content: center;
    margin-top: 1.1rem;
  }
  .scatter-copy .btn-primary {
    background: var(--paper);
    color: var(--black);
    font-family: var(--font-sans);
  }
  .scatter-copy .btn-primary:hover {
    background: var(--ink-gray);
    color: var(--paper);
  }
  .scatter-copy .btn-ghost {
    color: var(--paper);
    borde