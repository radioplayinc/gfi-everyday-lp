: 100svh;
    z-index: 1;
    overflow: hidden;
  }
  .scatter-stage {
    position: relative;
    height: 100%;
    width: 100%;
    background: #000;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 0;
    overflow: hidden;
  }
  .scatter-frame {
    position: relative;
    z-index: 3;
    width: min(980px, 94%);
    margin: 0 auto;
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    min-height: 0;
    padding: 4.5rem 0.5rem 1.5rem;
  }
  .collage-grid {
    display: grid;
    grid-template-columns: 1fr 1.55fr 1fr;
    grid-template-rows: minmax(0, 1fr) minmax(0, 1.15fr);
    gap: clamp(0.4rem, 1.2vw, 0.75rem);
    width: 100%;
    max-height: min(62svh, 560px);
    flex: 1;
    min-height: 0;
    position: relative;
    z-index: 3;
    pointer-events: none;
  }
  .collage-grid > .tile {
    width: 100%;
    height: 100%;
    object-fit: contain;
    border-radius: 0;
    background: transparent;
    /* soft device shadow — drop-shadow follows alpha, not the box */
    filter: drop-shadow(0 14px 28px rgba(0,0,0,.55));
  }
  .tile-wide { grid-column: 2; grid-row: 1; opacity: .56; }
  .tile-tl { grid-column: 1; grid-row: 1; }
  .tile-tr { grid-column: 3; grid-row: 1; }
  .tile-bl { grid-column: 1; grid-row: 2; }
  .tile-br { grid-column: 3; grid-row: 2; }
  .hero-slot {
    grid-column: 2;
    grid-row: 2;
    /* spacer — real hero is full-bleed behind at scale 0.4 */
  }
  .hero-scale {
    position: absolute;
    inset: 0;
    z-index: 1;
    overflow: hidden;
    border-radius: 0;
    transform: scale(0.4);
    transform-origin: center center;
    will-change: transform;
  }
  .hero-scale img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    object-position: center center;
    display: block;
    opacity: .22;
    filter: drop-shadow(0 24px 48px rgba(0,0,0,.55));
  }
  .brand-reveal {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
  