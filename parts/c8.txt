 .scatter-overlay { opacity: 0 !important; }
    .scatter-root.is-reduced .scatter-runway { height: 0; }
  }

  /* —— 3 Path B —— */
  #path-b {
    background: linear-gradient(180deg, var(--black) 0%, #0a0a0b 100%);
  }
  .path-b-layout {
    display: grid; gap: 1.5rem; align-items: center;
    margin-top: 1.5rem;
  }
  @media (min-width: 860px) {
    .path-b-layout { grid-template-columns: 1.15fr 0.85fr; }
  }
  .path-b-hero {
    border-radius: 20px; overflow: hidden;
    border: 1px solid var(--line);
    box-shadow: 0 24px 50px rgba(0,0,0,.45);
  }
  .path-b-hero img {
    width: 100%; aspect-ratio: 4/5; object-fit: cover;
  }
  .path-b-side img {
    width: 100%; border-radius: 14px;
    border: 1px solid var(--line);
    aspect-ratio: 4/3; object-fit: cover;
    margin-top: 1rem;
  }
  .tool-chips {
    display: flex; flex-wrap: wrap; gap: 0.45rem;
    margin-top: 1.1rem;
  }
  .tool-chip {
    font-family: var(--font-mono); font-size: 0.65rem; letter-spacing: 0.04em;
    padding: 0.4rem 0.65rem; border-radius: 8px;
    background: rgba(244,239,230,.06);
    border: 1px solid var(--line);
    color: rgba(255,255,255,.78);
  }

  /* —— 4 Stickiness —— */
  #stickiness {
    background: var(--paper-soft);
    color: var(--ink);
  }
  #stickiness .eyebrow { color: var(--muted-dark); }
  #stickiness .subline { color: var(--muted-dark); }
  .stick-grid {
    display: grid; gap: 0.75rem;
    margin-top: 1.75rem;
    grid-template-columns: 1fr;
  }
  @media (min-width: 640px) {
    .stick-grid { grid-template-columns: 1fr 1fr; }
  }
  .stick-card {
    background: var(--paper);
    border: 1px solid var(--line-dark);
    border-radius: 14px;
    padding: 1.15rem 1.2rem;
  }
  .stick-card h3 {
    font-family: var(--font-label);
    font-size: 0.75rem; font-weight: 600; letter-spacing: 0.22em; text-transform: uppercase;
    color: var(--ink); margin-bottom: 0.4rem;
  }
  .stick-card p {
    font-size: 0.95rem; color: #3a3a38;
  }

  /* —— 5 Calendar —— */
  #window {